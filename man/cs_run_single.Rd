% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cs-runner.R
\name{cs_run_single}
\alias{cs_run_single}
\title{Run a single DGP/estimator combination for one seed}
\usage{
cs_run_single(
  dgp_id,
  estimator_id,
  n,
  seed,
  version = NULL,
  status = "stable",
  quiet = FALSE,
  tau = cs_tau_oracle,
  bootstrap = FALSE,
  B = 0L,
  config = list(),
  board = NULL,
  max_runtime = Inf,
  ...
)
}
\arguments{
\item{dgp_id}{Character scalar, identifier of the DGP (e.g., "synth_baseline").}

\item{estimator_id}{Character scalar, identifier of the estimator (e.g., "lm_att").}

\item{n}{Integer sample size for the run.}

\item{seed}{Integer seed for reproducibility.}

\item{version}{Optional character version string for the DGP. Defaults to latest stable via registry.}

\item{status}{Optional status filter for DGP lookup ("stable", "experimental", "deprecated", "invalidated").}

\item{quiet}{Logical; if \code{TRUE}, suppress warnings from DGP lookup (use with
care—defaults to \code{FALSE} to honor loud-warning protocol).}

\item{tau}{Numeric vector of quantile levels used by QST-capable estimators.
Defaults to \link{cs_tau_oracle}.}

\item{bootstrap}{Logical; runner-level convenience flag. When \code{TRUE} and
\code{config$ci_method} is missing, the runner sets it to \code{"bootstrap"} and also
injects \code{config$seed} from \code{seed} (see \link{cs_ci_methods}).}

\item{B}{Integer; convenience alias for \code{config$n_boot} when \code{bootstrap=TRUE}.
Only used if \code{config$n_boot} is missing.}

\item{config}{List of estimator configuration options. Common fields include
\code{ci_method}, \code{n_boot}, and estimator-specific hyperparameters. See
\link{cs_ci_methods} for CI semantics.}

\item{board}{Optional pins board. If provided, the completed structured
result is persisted before it is returned.}

\item{max_runtime}{Numeric scalar; maximum allowed runtime (seconds) for the
estimator call. Defaults to \code{Inf}.}

\item{...}{Additional arguments forwarded to the estimator generator.}
}
\value{
A structured result list with \code{outputs}, canonical typed \code{scores},
compatibility projections \code{att} and \code{qst}, optional \code{boot_draws}, and
separate \code{meta} and \code{provenance} components.
}
\description{
Run a single DGP/estimator combination for one seed
}
