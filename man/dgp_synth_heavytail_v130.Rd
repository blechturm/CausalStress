% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgp-synth-heavytail.R
\name{dgp_synth_heavytail_v130}
\alias{dgp_synth_heavytail_v130}
\title{Heavy-tailed synthetic DGP for CausalStress (v1.3.0)}
\usage{
dgp_synth_heavytail_v130(
  n,
  seed = NULL,
  include_truth = TRUE,
  oracle_only = FALSE
)
}
\arguments{
\item{n}{Integer, number of observations.}

\item{seed}{Optional seed for reproducibility (passed to \code{cs_set_rng()}).}
}
\value{
A list with:
\itemize{
\item df: tibble with columns \code{y}, \code{w}, \code{y0}, \code{y1}, \code{p}, \code{structural_te},
\code{X1}, \code{X2}, \code{X3}, \code{X4}, \code{X5}
\item true_att: numeric scalar (structural ATT for treated units)
\item true_qst: tibble with columns \code{tau} and \code{value} (QST truth on
\code{cs_tau_oracle} grid)
\item meta: list with fields \code{dgp_id = "synth_heavytail"}, \code{type = "synthetic"},
\code{structural_te = tau}
}
}
\description{
Implements the \code{synth_heavytail} design from the DGP registry. This DGP
shares the same covariates, baseline outcome, treatment effect, and
propensity as \code{synth_baseline}, but replaces the Gaussian noise with a
heavy-tailed mixture:
epsilon ~ 0.8 * N(0, 0.5) + 0.2 * Cauchy(0, 1).
}
\details{
\strong{Goal:} Penalize non-robust loss functions (e.g., L2/MSE).

\strong{Structure:} Same \eqn{\mu_0}, \eqn{\tau}, and \eqn{p} as \code{synth_baseline}.

\strong{Noise mixture:} \eqn{\varepsilon \sim 0.8 \cdot \mathcal{N}(0, 0.5) + 0.2 \cdot \text{Cauchy}(0, 1)}.

\strong{Note:} Variance is undefined; winsorization is forbidden inside the DGP generation.
}
