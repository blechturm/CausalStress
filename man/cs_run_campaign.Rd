% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cs-run-campaign.R
\name{cs_run_campaign}
\alias{cs_run_campaign}
\title{Run a full campaign across DGPs, estimators, and seeds}
\usage{
cs_run_campaign(
  dgp_ids = NULL,
  estimator_ids = NULL,
  dgp_id = NULL,
  estimator_id = NULL,
  seeds,
  n,
  defaults = list(),
  overrides = list(),
  campaign_seed = NULL,
  version = NULL,
  status = "stable",
  tau = cs_tau_oracle,
  bootstrap = FALSE,
  B = 0L,
  skip_existing = FALSE,
  board = NULL,
  staging_dir = NULL,
  parallel = FALSE,
  experimental_parallel = FALSE,
  show_progress = TRUE,
  force = FALSE,
  quiet = TRUE,
  max_runtime = Inf,
  ...
)
}
\arguments{
\item{dgp_ids}{Character vector of DGP IDs.}

\item{estimator_ids}{Character vector of estimator IDs.}

\item{dgp_id}{Deprecated alias for \code{dgp_ids} (scalar character).}

\item{estimator_id}{Deprecated alias for \code{estimator_ids} (scalar character).}

\item{seeds}{Integer vector of seeds.}

\item{n}{Integer sample size per run.}

\item{defaults}{Optional default estimator config list forwarded to
\code{cs_run_single()} (e.g., \code{list(n_boot = 200, num_threads = 1L)}).}

\item{overrides}{Optional named list of estimator-specific config overrides
(e.g., \code{list(tmle_att = list(ci_method = "native"))}). Each override is
merged over \code{defaults} using \code{utils::modifyList()}.}

\item{campaign_seed}{Optional scalar integer used to deterministically
shuffle task execution order for better load balancing without relying on
ambient RNG state. If \code{NULL}, tasks are executed in deterministic grid
order.}

\item{skip_existing}{Logical; if TRUE, skip tasks already pinned on \code{board}.}

\item{board}{Optional pins board for persistence.}

\item{staging_dir}{Optional staging directory for crash recovery.}

\item{parallel}{Logical; if TRUE, uses furrr/future for parallel execution.}

\item{show_progress}{Logical; show progressr-based progress.}

\item{...}{Additional arguments forwarded to cs_run_single() (tau, etc.).}
}
\value{
Tibble with one row per run.
}
\description{
This runner parallelizes over the full Cartesian product of
(dgp_id, estimator_id, seed) with dynamic load balancing, making it
the recommended entry point for large heterogeneous campaigns.
}
