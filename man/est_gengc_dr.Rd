% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/est-gengc-dr.R
\name{est_gengc_dr}
\alias{est_gengc_dr}
\title{Doubly robust GenGC estimator (soft dependency)}
\usage{
est_gengc_dr(df, tau = cs_tau_oracle, config = list())
}
\arguments{
\item{df}{Data frame with \code{y}, \code{w}, and covariates; oracle columns (\code{y0},
\code{y1}, \code{p}, \code{structural_te}) are dropped before calling GenGC.}

\item{tau}{Numeric vector of quantile levels passed to \code{tau_grid}.}

\item{config}{Optional list: \code{n_draws} (default 300), \code{num_trees} (default
800), \code{crossfit_folds} (default 5), \code{num_threads} (default 1, enforced).}
}
\value{
List with \code{att}, \code{qst}, \code{cf = NULL}, and \code{meta} satisfying
\code{cs_check_estimator_output(require_qst = TRUE)}.
}
\description{
Adapter around GenGC's doubly robust estimator. Mirrors \code{est_gengc} but
uses \code{GenGC::gengc_dr} with cross-fitting while respecting the airlock and
single-thread contract.
}
