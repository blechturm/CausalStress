% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/est-oracle-att.R
\name{est_oracle_att}
\alias{est_oracle_att}
\title{Oracle ATT estimator using structural treatment effects}
\usage{
est_oracle_att(df, tau = cs_tau_oracle, config = list())
}
\arguments{
\item{df}{A data.frame or tibble containing at least \code{w} and
\code{structural_te}. Typically this is \code{dgp$df} from a synthetic DGP.}

\item{tau}{Numeric vector of quantile levels. Ignored here but kept for
interface compatibility with the estimator contract.}

\item{config}{List of estimator-specific settings. Ignored here except
for being stored in the returned metadata.}
}
\value{
A list with components \code{att}, \code{qst}, \code{cf}, and \code{meta}, satisfying
the estimator contract used in CausalStress.
}
\description{
This estimator uses the structural treatment effect \code{structural_te}
(τ(X)) to compute the ATT on treated units. It is intended only for
internal testing and validation, not for real benchmarking.
}
