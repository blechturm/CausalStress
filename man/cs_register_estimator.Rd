% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cs-estimator-registry.R
\name{cs_register_estimator}
\alias{cs_register_estimator}
\title{Register an additional estimator at runtime}
\usage{
cs_register_estimator(
  estimator_id,
  type,
  generator,
  oracle = FALSE,
  supports_qst = FALSE,
  version = "0.0.0-local",
  description = "",
  source = "external",
  requires_pkgs = character(0)
)
}
\arguments{
\item{estimator_id}{Character scalar, unique identifier for the estimator.}

\item{type}{Character scalar describing the estimator family
(e.g. "gcomp", "ipw", "dr", "rf", "external").}

\item{generator}{Function that implements the estimator, taking at least
arguments \verb{(df, config = list(), tau = cs_tau_oracle, ...)}.}

\item{oracle}{Logical, whether this is an oracle estimator.}

\item{supports_qst}{Logical, whether the estimator returns QST values.}

\item{version}{Character scalar, version string for the estimator implementation.
Defaults to \code{"0.0.0-local"}.}

\item{description}{Character scalar, short human-readable description.}

\item{source}{Character scalar indicating where the estimator comes from,
e.g. "external", "optional". Defaults to \code{"external"}.}

\item{requires_pkgs}{Character vector of package names that must be installed
for the estimator to work. Defaults to \code{character(0)}.}
}
\value{
Invisibly, the updated registry tibble (core + extra).
}
\description{
This function allows external packages or user code to register new estimators
into the CausalStress estimator registry. Registered estimators must obey the
estimator contract: the \code{generator} function must accept \verb{(df, config, tau, ...)}
and return a list compatible with \code{cs_check_estimator_output()}.
}
