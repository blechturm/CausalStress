% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cs-summary.R
\name{cs_summarise_runs}
\alias{cs_summarise_runs}
\title{Summarise Monte Carlo runs for a DGP × estimator combination}
\usage{
cs_summarise_runs(runs)
}
\arguments{
\item{runs}{A tibble or data.frame, typically the output of
\code{\link[=cs_run_seeds]{cs_run_seeds()}}, with columns including:
\code{dgp_id}, \code{estimator_id}, \code{n}, \code{seed}, \code{oracle}, \code{supports_qst},
\code{true_att}, \code{est_att}, \code{att_error}, \code{att_abs_error}.}
}
\value{
A tibble with one row and summary columns:
\itemize{
\item dgp_id, estimator_id, n, oracle, supports_qst
\item n_runs: number of seeds / runs
\item mean_true_att, mean_est_att
\item mean_error, sd_error
\item mean_abs_error, max_abs_error
}

If multiple (dgp_id, estimator_id, n) combinations are present, the
function summarises within each group and returns one row per group.
}
\description{
This helper takes the output of \code{\link[=cs_run_seeds]{cs_run_seeds()}} (or any tibble with the
same columns) and computes Monte Carlo summary statistics for the ATT
error. The input is expected to contain one row per seed.
}
