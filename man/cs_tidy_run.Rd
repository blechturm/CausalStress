% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cs-tidy-run.R
\name{cs_tidy_run}
\alias{cs_tidy_run}
\title{Convert a single CausalStress run to a one-row tibble}
\usage{
cs_tidy_run(x)
}
\arguments{
\item{result}{A list as returned by \code{\link[=cs_run_single]{cs_run_single()}}.}
}
\value{
A tibble with a single row and the same columns as
\code{\link[=cs_run_seeds]{cs_run_seeds()}}, e.g.:
\code{dgp_id}, \code{estimator_id}, \code{n}, \code{seed}, \code{oracle}, \code{supports_qst},
\code{true_att}, \code{est_att}, \code{att_error}, \code{att_abs_error},
\code{att_ci_lo}, \code{att_ci_hi}, \code{boot_covered}, \code{n_boot_ok},
\code{estimator_pkgs}, \code{log}, etc.
}
\description{
\code{cs_tidy_run()} is a convenience helper that flattens the rich
list returned by \code{\link[=cs_run_single]{cs_run_single()}} into a one-row tibble, using
the same column structure as \code{\link[=cs_run_seeds]{cs_run_seeds()}} and \code{\link[=cs_run_grid]{cs_run_grid()}}.
}
\details{
This is useful when:
\itemize{
\item you call \code{\link[=cs_run_single]{cs_run_single()}} interactively for debugging or small experiments,
\item and want results that are directly comparable to multi-seed campaigns.
}
}
