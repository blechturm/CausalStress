% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cs-runner-grid.R
\name{cs_run_grid}
\alias{cs_run_grid}
\title{Run a DGP × estimator grid over multiple seeds}
\usage{
cs_run_grid(
  dgp_ids,
  estimator_ids,
  n,
  seeds,
  tau = NULL,
  bootstrap = FALSE,
  B = 200L,
  config = list()
)
}
\arguments{
\item{dgp_ids}{Character vector of DGP IDs (must exist in cs_dgp_registry()).}

\item{estimator_ids}{Character vector of estimator IDs (must exist in cs_estimator_registry()).}

\item{n}{Integer, sample size per run (same for all combinations).}

\item{seeds}{Integer vector of seeds to run for each (DGP, estimator) pair.}

\item{tau}{Optional numeric vector of quantile levels (forwarded to cs_run_single()).}

\item{config}{Optional named list of configuration settings (forwarded to cs_run_single()).}
}
\value{
A tibble with one row per (dgp_id, estimator_id, seed) run, including
all columns from cs_run_single(), and bindable into cs_summarise_runs().
}
\description{
Run a DGP × estimator grid over multiple seeds
}
