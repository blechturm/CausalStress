% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cs-runner-grid.R
\name{cs_run_grid}
\alias{cs_run_grid}
\title{Run a DGP × estimator grid over multiple seeds}
\usage{
cs_run_grid(
  dgp_ids,
  estimator_ids,
  n,
  seeds,
  tau = NULL,
  version = NULL,
  status = "stable",
  bootstrap = FALSE,
  B = 200L,
  config = list(),
  board = NULL,
  skip_existing = FALSE,
  force = FALSE,
  max_runtime = Inf,
  parallel = FALSE,
  experimental_parallel = FALSE,
  staging_dir = NULL,
  show_progress = FALSE
)
}
\arguments{
\item{dgp_ids}{Character vector of DGP IDs (must exist in cs_dgp_registry()).}

\item{estimator_ids}{Character vector of estimator IDs (must exist in cs_estimator_registry()).}

\item{n}{Integer, sample size per run (same for all combinations).}

\item{seeds}{Integer vector of seeds to run for each (DGP, estimator) pair.}

\item{tau}{Optional numeric vector of quantile levels (forwarded to cs_run_single()).}

\item{version}{Optional DGP version string; forwarded to \code{\link[=cs_get_dgp]{cs_get_dgp()}}.}

\item{status}{Optional DGP status filter; forwarded to \code{\link[=cs_get_dgp]{cs_get_dgp()}}.}

\item{bootstrap}{Logical, whether to run bootstrap for ATT CIs.}

\item{B}{Integer, number of bootstrap replicates.}

\item{config}{Optional named list of configuration settings (forwarded to cs_run_single()).}

\item{board}{Optional pins board for persistence.}

\item{skip_existing}{Logical, whether to resume from existing pins.}

\item{force}{Logical, whether to overwrite existing pins (alias for
\code{skip_existing = FALSE}).}
}
\value{
A tibble with one row per (dgp_id, estimator_id, seed) run, including
all columns from cs_run_single(), and bindable into cs_summarise_runs().
}
\description{
Run a DGP × estimator grid over multiple seeds
}
