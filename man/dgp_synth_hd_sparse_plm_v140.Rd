% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgp-synth-hd-sparse-plm.R
\name{dgp_synth_hd_sparse_plm_v140}
\alias{dgp_synth_hd_sparse_plm_v140}
\title{High-dimensional sparse partially linear DGP (v1.4.0)}
\usage{
dgp_synth_hd_sparse_plm_v140(
  n,
  seed = NULL,
  include_truth = TRUE,
  oracle_only = FALSE
)
}
\arguments{
\item{n}{Integer, number of observations.}

\item{seed}{Optional seed for reproducibility (passed to \code{cs_set_rng()}).}

\item{include_truth}{Logical; if TRUE, include oracle truth tables where supported.}

\item{oracle_only}{Logical; if TRUE, return only columns needed for oracle truth generation where supported.}
}
\value{
A synthetic DGP list with df, true_att, true_qst, and meta.
}
\description{
Version bump that increases dimensionality and hardens collinearity:
\eqn{X \in \mathbb{R}^{100}} with Toeplitz correlation
\eqn{\Sigma_{ij} = 0.95^{|i-j|}}.
}
\details{
The outcome regression remains sparse in the first 5 covariates, but the
propensity signal is strengthened (still sparse) to push standard propensity
modeling into a separation-like regime when \(N/P\) is small.
}
