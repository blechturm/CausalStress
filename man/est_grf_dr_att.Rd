% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/est-grf-dr.R
\name{est_grf_dr_att}
\alias{est_grf_dr_att}
\alias{est_grf_dr}
\title{GRF-based doubly-robust ATT estimator (causal forest)}
\usage{
est_grf_dr_att(df, config = list(), tau = cs_tau_oracle, ...)

est_grf_dr(df, config = list(), tau = cs_tau_oracle, ...)
}
\arguments{
\item{df}{Data frame as returned in \code{dgp$df}, containing at least
\code{y}, \code{w}, and covariates. Covariates are taken as all non-core columns
(i.e., excluding \code{y}, \code{w}, \code{y0}, \code{y1}, \code{p}, \code{structural_te}).}

\item{config}{Optional list of configuration options. Common fields include:
\itemize{
\item \code{ci_method}: CI intent; one of "none", "default", "bootstrap", "native" (see \link{cs_ci_methods}).
\item \code{seed}: required when bootstrap CIs are requested.
\item \code{n_boot}: number of bootstrap draws if using bootstrap CIs.
\item GRF hyperparameters (e.g., \code{num.trees}, \code{min.node.size}).
}
For this estimator, \code{ci_method = "default"} maps to \code{"native"}.}

\item{tau}{Ignored. Included for API compatibility with other estimators.}

\item{...}{Ignored. Reserved for future extensions.}
}
\value{
A legacy-compatible estimator result containing an \code{att} estimate
and optional interval, \code{qst = NULL}, \code{cf = NULL}, and CI/provenance \code{meta}.
}
\description{
This estimator wraps the \code{grf::causal_forest()} implementation to estimate
the average treatment effect on the treated (ATT). It is treated as
"doubly-robust" in the sense of GRF: the causal forest internally uses
orthogonalization with nuisance estimates for propensity and outcome.
}
\details{
This estimator is an optional registry entry and requires the \code{grf} package.
The descriptor is discoverable without \code{grf}; execution requires the package.
}
